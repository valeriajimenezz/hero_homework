# hero homework 

# How to Compile and Run
To compile the project via the terminal using the C++11 standard, run the following command:
```bash
g++ -std=c++11 main.cpp -o game

# After compiling, you can run the executable:
windows: ./game.exe
Linux/mac:./game
